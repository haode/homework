#include <iostream>
#include <string>

int main(){
for (int i = 0; i < size(); i++)
	Recognition reader = new Recognition;
	string barcode, number;
	reader.recognize(a[i], &barcode, &number);
	GoodsDAO* dao = new GoodsDAO(new Recognition);
	dao


}